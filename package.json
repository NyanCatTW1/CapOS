{
  "devDependencies": {
    "gts": "^3.1.0",
    "typescript": "^4.7.4",
    "xterm": "^4.18.0",
    "@types/node": "^18.0.0"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },

  "engines": {
    "node": ">=12.0.0"
  }
}
